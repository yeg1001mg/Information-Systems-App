export enum Links {
    Home = '/home',
    UserProfile = '/profile',
    // UserProfile = '/profile/:uid',
    SystemsTable = '/systems-table',
    Login = '/login',
    SignUp = '/sign-up',
    NonExistent = '*',
}
